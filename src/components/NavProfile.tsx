import { User } from "lucide-react";

export default function NavProfile() {
  return (
    <div className="hover:bg-gray-100 rounded-full lg:p-2 p-[0.2rem] border">
      <User size={18} />
    </div>
  );
}
